import './style.css'

/**
 * Displays the wireframe of a part of the applicaiton.
 *
 * @returns {JSX.Element} - The Loader component.
 */
export default function Loader({ children }) {
  return <div className="Loader">{children}</div>
}
